#!/bin/bash

if [[ ! -d $1 ]]; then
  echo "Usage: $0 <dir containing images>"
  exit 1
fi

export SWWW_TRANSITION_FPS=60
export SWWW_TRANSITION_STEP=2
export SWWW_TRANSITION_TYPE=wipe

INTERVAL=1800

while true; do
  swww img $BG_DIR/$(ls -1 $BG_DIR | shuf -n 1)
  sleep $INTERVAL
done
